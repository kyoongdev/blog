import { atom } from 'jotai';

export const fusbleRef = atom<HTMLLIElement | null>(null);
export const humonlabRef = atom<HTMLLIElement | null>(null);
export const projectRef = atom<HTMLLIElement | null>(null);
